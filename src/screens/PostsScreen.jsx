import React from "react";
import { View, Text } from "react-native";
import { useIsFocused } from "@react-navigation/native";

const PostsScreen = () => {
  //   const isFocused = useIsFocused();
  return <Text>render something here</Text>;
};

export default PostsScreen;
